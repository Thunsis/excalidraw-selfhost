<p align="center">
  <img src="https://raw.githubusercontent.com/Thunsis/excalidraw-selfhost/main/docs/logo.svg" width="120" alt="excalidraw-selfhost logo">
</p>

<h1 align="center">excalidraw-selfhost</h1>

<p align="center">
  <b>Your personal Excalidraw cloud — scenes, library and AI text-to-diagram on your own infrastructure.</b><br>
  Plus a pure-Node CLI that draws the same diagrams from the terminal.
</p>

<p align="center">
  <a href="https://github.com/Thunsis/excalidraw-selfhost/blob/main/LICENSE"><img src="https://img.shields.io/badge/license-PolyForm%20Noncommercial-blue.svg" alt="License: PolyForm Noncommercial"></a>
  <a href="#"><img src="https://img.shields.io/badge/platform-macOS-lightgrey.svg" alt="Platform: macOS"></a>
  <a href="#"><img src="https://img.shields.io/badge/node-%3E%3D24-green.svg" alt="Node >= 24"></a>
  <a href="https://github.com/Thunsis/excalidraw-selfhost"><img src="https://img.shields.io/github/stars/Thunsis/excalidraw-selfhost" alt="GitHub stars"></a>
  <a href="#"><img src="https://img.shields.io/badge/CLI-pure%20Node-4f46e5.svg" alt="CLI: pure Node"></a>
</p>

<p align="center">
  <a href="#why">Why</a> ·
  <a href="#features">Features</a> ·
  <a href="#gallery">Gallery</a> ·
  <a href="#quick-start">Quick Start</a> ·
  <a href="#cli">CLI</a> ·
  <a href="#architecture">Architecture</a> ·
  <a href="#ux--auth">UX & Auth</a> ·
  <a href="#project-layout">Layout</a> ·
  <a href="#data--privacy">Privacy</a> ·
  <a href="#faq">FAQ</a> ·
  <a href="#license">License</a>
</p>

---

## Why

Excalidraw stores everything in browser storage. [Excalidraw+](https://plus.excalidraw.com) — the official paid tier — adds cloud sync, but it's SaaS with no self-host path (see [excalidraw/excalidraw#1772](https://github.com/excalidraw/excalidraw/issues/1772), open since 2020).

Existing self-host projects (alswl/excalidraw-collaboration et al.) focus on **real-time collaboration**. This project fills the other gap: a **personal cloud workspace** — your scenes, your library, your AI assistant, on your own infra. All data stays on your machine. And because the AI pipeline is fully official, the terminal gets the exact same drawing engine as the in-app AI.

## Features

| | excalidraw-selfhost | alswl/excalidraw-collaboration | Excalidraw+ (paid) |
|---|---|---|---|
| **Sign in** (username as key) | ✅ | ❌ | ✅ |
| **Cloud scenes** (per-user, SQLite) | ✅ | ❌ | ✅ |
| **Cloud library sync** | ✅ | ❌ | ✅ |
| **AI text-to-diagram** (self-hosted) | ✅ | ❌ | ✅ |
| **HTTPS by default** (auto-redirect + HSTS) | ✅ | ❌ | ✅ |
| **`excalidraw` CLI** (pure Node, no browser) | ✅ | ❌ | ❌ |
| **CLI PNG export** (official `exportToSvg` + resvg) | ✅ | ❌ | ❌ |
| Real-time collaboration | ❌ (out of scope) | ✅ | ✅ |
| **Data ownership** | ✅ 100% | ✅ | ❌ |

> **Scope**: this is a **personal cloud** tool — one user's drawings, library & AI, synced across devices, fully owned. Real-time multi-user collaboration is deliberately out of scope.

- **Single-source monorepo** — the upstream Excalidraw repo stays pristine; customization lives as a versioned patch set (`patch/`), applied at build time.
- **Official pipeline everywhere** — conversion uses the exact official packages the frontend AI uses (`@excalidraw/mermaid-to-excalidraw@2.2.2`, `@excalidraw/element@0.18.0`), and PNG export uses the official `exportToSvg`. No reinvented converters.
- **WAL-safe backups & healthcheck** — `make backup`, `make check`, cron-friendly.
- **Tunnel-ready** — Cloudflare Tunnel support for private HTTPS access without opening ports.

## Gallery

Every diagram below was generated by the CLI (`excalidraw mermaid` → official pipeline → `excalidraw export --png`) — CJK handwritten font, rough hand-drawn layout, identical to the in-app AI.

<img src="https://raw.githubusercontent.com/Thunsis/excalidraw-selfhost/main/docs/cli-example.png" alt="Bilingual flowchart generated by the CLI" width="520">
<sup>Bilingual flowchart — `excalidraw mermaid` with real font metrics, exported straight to PNG.</sup>

<img src="https://raw.githubusercontent.com/Thunsis/excalidraw-selfhost/main/docs/ux-intents.png" alt="UX intent dispatch diagram" width="420">
<sup>UX intent dispatch — two intents, two dialogs (see <a href="#ux--auth">UX & Auth</a>).</sup>

<img src="https://raw.githubusercontent.com/Thunsis/excalidraw-selfhost/main/docs/ux-auth-sequence.png" alt="Auth state sequence diagram" width="560">
<sup>Auth state sequence — login pulls cloud data, sign-out falls back to local.</sup>

## Quick Start

> **Requirements**: macOS (launchd) · Node ≥ 24 · a local clone of [excalidraw/excalidraw](https://github.com/excalidraw/excalidraw) (pinned `1acf66e`) · optional Cloudflare account

**1. Install**

```bash
git clone https://github.com/Thunsis/excalidraw-selfhost.git
cd excalidraw-selfhost
cp .env.example .env            # fill in your values
make install                    # symlink launchd agents + configs
```

**2. Bootstrap the frontend** (one-time, per Excalidraw repo)

```bash
cd /path/to/excalidraw          # pinned commit 1acf66e
git apply /path/to/excalidraw-selfhost/patch/excalidraw.patch
cp -R /path/to/excalidraw-selfhost/patch/new/ .
corepack yarn build             # → excalidraw-app/build/ (served by caddy)
```

**3. Start & open**

```bash
for job in ws-backend ai-backend caddy cloudflared; do
  launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.excalidraw.$job.plist
done
# open https://localhost:3001 and sign in with any username
```

**Install the CLI** (optional but recommended):

```bash
cd excalidraw-selfhost/cli
npm install && npm run fonts    # woff2 slices → ttf for PNG export
npm link                        # → `excalidraw` on PATH
excalidraw list                 # your cloud scenes
```

### Frontend development workflow

Patches are the **single source of truth**. The Excalidraw repo stays clean (0 uncommitted changes):

```bash
make build            # restore → patch → build → restore (in the excalidraw repo)
make patch-export     # after editing code: re-export excalidraw.patch + new/
```

### Auth model

Deliberately simple: **username IS the credential** — no passwords, registration closed. Perfect for private self-hosting; **do not expose to the public internet** without adding real auth.

## CLI

The `excalidraw` CLI creates cloud scenes without opening the browser — and exports them as PNGs without one either.

```
excalidraw list [--json]                     list cloud scenes
excalidraw show <id> [--json]                scene detail (elements)
excalidraw create <name> [elements.json]     create scene from elements JSON
excalidraw update <id> [--name N] [--elements file.json]
excalidraw delete <id>
excalidraw ai "<prompt>" [--name N] [--dry-run] [--verbose] [--local]
excalidraw mermaid <file.mmd> [--name N] [--local]
excalidraw export <id> [out.json] [--png]   elements JSON, or PNG image with --png
excalidraw library get|put <file.json>       cloud library
excalidraw doctor                            env & connectivity check
```

```bash
excalidraw ai "用户登录流程" --name "login flow"   # LLM → mermaid → scene
excalidraw mermaid flow.mmd                        # mermaid file → scene
excalidraw export 42 scene.png --png               # scene → PNG (pure Node)
```

### Pure-Node pipeline — the same engine as the frontend AI

<img src="https://raw.githubusercontent.com/Thunsis/excalidraw-selfhost/main/docs/cli-pipeline.png" alt="CLI conversion pipeline" width="560">

- **Conversion**: `mermaid official render (jsdom)` → `@excalidraw/mermaid-to-excalidraw` → `@excalidraw/element convertToExcalidrawElements` — the exact chain the in-app AI runs, zero custom converters. jsdom is the same environment mermaid's own CI uses.
- **Measurement is real font data**: `cli/src/metrics.js` reads the system Trebuchet MS hmtx table via opentype.js — the same metrics the browser's `getBBox` returns. No magic coefficients.
- **PNG export**: official `exportToSvg` (esbuild-bundled from `@excalidraw/excalidraw`) → resvg rasterization, with the bundled Excalifont handwritten font (7 OFL-licensed slices, `fonts/ttf/`). No browser, no playwright.
- **Same CJK handwriting in-browser**: the patched frontend preloads Xiaolai font slices (`Fonts.loadElementsFonts` after `updateScene`), so CLI-made scenes open with the handwritten look.

### Diagram type support

| Type | CLI (pure Node) | Browser (frontend AI) |
|---|---|---|
| flowchart (TD/LR/BT/RL, 中文/English, long text, diamonds) | ✅ full, pixel-aligned with browser | ✅ full |
| sequenceDiagram | ✅ converts (edge points null is an official converter quirk, renders fine) | ✅ same |
| classDiagram / erDiagram / stateDiagram / gantt | ⚠️ rejected with a clear error (official converter falls back to graphImage in browsers too) | ⚠️ same fallback |

Non-flowchart types are a limitation of the official `mermaid-to-excalidraw` converter (it queries the rendered SVG with ids mermaid 11 no longer emits) — the CLI fails loudly instead of silently saving a raster image.

### Verification

```bash
cd cli
node tests/regression-official.js   # 7 cases: flowchart strict + official-boundary types
```

Pure-Node output is verified element-by-element against a real-browser run (via mcphub → playwright MCP, verification only): shape sizes within ~4%, y-range within ~2%.

## Architecture

<img src="https://raw.githubusercontent.com/Thunsis/excalidraw-selfhost/main/docs/arch.png" alt="System architecture" width="640">

- **ws-server** (3020) — JWT auth, scenes CRUD, per-user key-value data (library, AI chats), share links. Express + better-sqlite3.
- **ai-server** (3016) — SSE proxy for Excalidraw's Text-to-Diagram → any OpenAI-compatible endpoint (default `deepseek-v4-flash` via opencode.ai).
- **caddy** (3001) — serves the patched frontend build + reverse-proxies `/ws-api` and `/ai-proxy`. Self-signed HTTPS for HTTP/2, **auto-redirects plain HTTP to HTTPS** (via `Cf-Visitor`) and sends **HSTS** so browsers pin the secure connection.
- **cloudflared** — optional Cloudflare Tunnel: no open ports, no DNS setup.
- **excalidraw CLI** — talks to the same `ws-server` and `ai-server` APIs; scenes created from the terminal appear in your cloud workspace immediately.

## UX & Auth

The frontend's cloud UX is built on three deliberate design decisions.

### 1. One auth store — the single source of truth

All token reads go through a module-level auth store (`workspaceCloud.ts`: `getToken` / `setAuth` / `clearAuth`). No component reads `localStorage` directly — sign-out can only happen through the UI, so every consumer stays consistent by construction.

### 2. Two intents, two dialogs

"Sign in" and "Open workspace" are different jobs, so they are separate events and separate dialogs:

<img src="https://raw.githubusercontent.com/Thunsis/excalidraw-selfhost/main/docs/ux-intents.png" alt="Two intents, two dialogs" width="420">

- `ws:open-signin` → **SignInDialog** (pure login — no scene list, no file UI).
- `ws:open-workspace` → **WorkspaceDialog** (cloud scenes with remove, plus Open file…).
- All 15 entry points (top bar, welcome screen, hamburger menu, Save to Cloud, command palette, AI tools…) dispatch the *right* intent instead of one generic panel.

### 3. Library & AI follow the login state

<img src="https://raw.githubusercontent.com/Thunsis/excalidraw-selfhost/main/docs/ux-auth-sequence.png" alt="Library and AI follow the login state" width="560">

Login pulls cloud library & AI chats in; sign-out falls back to local IndexedDB and unbinds the current scene — the canvas keeps its content but stops auto-saving to the cloud.

## Project Layout

```
apps/            deployable applications (each self-contained)
  ws-server/     cloud workspace backend (Express + better-sqlite3 + JWT)
  ai-server/     AI text-to-diagram SSE proxy
cli/             excalidraw CLI (official pipeline, PNG export, tests/, fonts/)
patch/           frontend customization delta (excalidraw.patch + new/)
deploy/          platform-specific assembly (launchd / caddy / cloudflared)
scripts/         ops toolchain (install / apply-patch / build / backup / healthcheck)
docs/            upgrade guide, troubleshooting, generated diagrams
Makefile         unified entry point (make install / build / patch / backup / check / doctor)
```

## Data & Privacy

- **All user data lives in SQLite** at `apps/ws-server/data/` (default `WS_DATA_DIR`) — scenes, library, chat history, users.
- That directory is **gitignored** (`.gitignore: apps/ws-server/data/`) — your drawings never enter the repo or GitHub.
- JWT secret auto-generates on first run at `<WS_DATA_DIR>/.jwt-secret` (mode `600`), persists across restarts.
- `make backup` — WAL-safe snapshot, keeps last 7. `make check` — healthcheck (silent when healthy, cron-friendly).

## FAQ

**Why not just use alswl/excalidraw-collaboration?**
Different goal. That project is built for real-time multi-user collaboration; this one is a **personal cloud** — your scenes, library and AI synced across your own devices. If you need collab, that project is the right choice; this one isn't trying to be it.

**Why is "Sign in" separate from "Open"?**
Because they are different actions with different failure modes. A clean login dialog (username only) removes friction, while the workspace dialog is about your scenes — and when signed out it says so and offers a sign-in link instead of showing a confusing empty list.

**Why PolyForm Noncommercial?**
Personal and non-commercial use is free; commercial use requires a license. The patched frontend still contains MIT-licensed upstream code (see [LICENSE](LICENSE)). Bundled Excalifont font slices are SIL OFL licensed.

**Why does the CLI reject class/er/state diagrams?**
That's the official `@excalidraw/mermaid-to-excalidraw` converter, not the CLI — in a real browser those types also fall back to a raster `graphImage` (mermaid 11 changed the SVG ids the converter queries). The CLI reports it instead of silently saving an image; flowchart and sequence work fully.

**Is the CLI's output identical to the in-app AI?**
Yes for what matters: same official packages, same versions (`mermaid-to-excalidraw@2.2.2`, `@excalidraw/element@0.18.0`), same jsdom environment mermaid's CI uses, and real font metrics instead of estimates. Verified element-by-element against a browser run (≤4% size delta).

**Can I use the CLI without the AI server?**
Yes — `excalidraw mermaid file.mmd` converts a local Mermaid file directly (no LLM involved). `excalidraw ai` needs the ai-server. Both need ws-server for the cloud account (`EXCALIDRAW_WS_USER`).

**Is my data backed up?**
`make backup` snapshots the SQLite DB WAL-safely (works while running) and keeps 7 generations. Pair with cron for daily backups.

**How do I upgrade when Excalidraw upstream moves?**
Deliberately slow (quarterly). See [docs/upgrade.md](docs/upgrade.md) — pull, resolve conflicts by hand, re-export the patch.

## Scope

This project is built around a single, clear goal: **your personal Excalidraw cloud**. One user, their scenes, their library, their AI — synced across devices, owned entirely by them.

Explicit non-goals (no plans to add):

- Real-time multi-user collaboration — use [alswl/excalidraw-collaboration](https://github.com/alswl/excalidraw-collaboration) if you need it.
- Multi-tenant SaaS / public hosting — usernames are the only credential, by design.

The codebase stays small and focused: a personal cloud is a solved problem in ~1,000 lines across two backends; adding collaboration would double the surface and complexity for a scenario this project doesn't target.

## License

[PolyForm Noncommercial 1.0.0](LICENSE) — © 2026 Thunsis. Personal & non-commercial use free; commercial use requires permission. Upstream Excalidraw code remains MIT-licensed; bundled Excalifont font slices are SIL OFL 1.1.
